BEGIN
	DECLARE jname, jdire TEXT;
    DECLARE juegos_cursor CURSOR FOR SELECT Nombre, Director FROM reviews;
    OPEN juegos_cursor;
    FETCH FROM juegos_cursor INTO jname, jdire;
    IF jname = "Minecraft" THEN 
    	SELECT jdire;
    END IF;
    CLOSE juegos_cursor;

END